# Non-secret runtime defaults upserted into the live profile .env on apply.
# Secrets (SLACK_*, tokens) never belong here.
HERMES_CODEX_EVENT_STALE_TIMEOUT_SECONDS=900
HERMES_CODEX_TTFB_TIMEOUT_SECONDS=300
HERMES_CODEX_TTFB_MAX_SECONDS=300
